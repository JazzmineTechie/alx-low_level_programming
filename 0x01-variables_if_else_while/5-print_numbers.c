#include <stdio.h>
/**
 * main - Entry point
 * Description: print all the digits in base ten starting from zero'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
